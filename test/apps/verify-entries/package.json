{
  "name": "@okta/test.apps.verify-entries",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=12.0"
  },
  "scripts": {
    "build:idx": "tsc && vite build --config vite.idx.config.js",
    "dev:idx": "tsc && vite dev --config vite.idx.config.js",
    "build:authn": "tsc && vite build --config vite.authn.config.js",
    "dev:authn": "tsc && vite dev --config vite.authn.config.js",
    "build:default": "tsc && vite build --config vite.default.config.js",
    "dev:default": "tsc && vite dev --config vite.default.config.js"
  },
  "dependencies": {
    "@okta/okta-auth-js": "*"
  },
  "devDependencies": {
    "typescript": "^4.7.3",
    "vite": "^2.9.16",
    "rollup-plugin-visualizer": "~5.5.4",
    "@okta/env": "*"
  }
}
