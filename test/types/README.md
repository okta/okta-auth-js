**Note:** All tests in this directory are running against types within 'build' folder, types should be generated before running test here.

Script to generate types:

```bash
yarn build
```

or

```bash
yarn build:types
```
