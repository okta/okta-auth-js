{
  "name": "@okta/generator",
  "version": "1.0.0",
  "description": "Generator for samples, docs and snippets",
  "private": true,
  "scripts": {
    "gulp": "HYGEN_OVERWRITE=1 gulp",
    "generate": "HYGEN_OVERWRITE=1 gulp",
    "generate:react": "HYGEN_OVERWRITE=1 gulp generate:react && gulp install",
    "dev:react": "HYGEN_OVERWRITE=1 gulp dev:react"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "dependencies": {
    "gulp": "^4.0.2",
    "hygen": "^6.1.0",
    "gulp-clean": "^0.4.0",
    "gulp-rename": "^2.0.0",
    "merge-stream": "^2.0.0",
    "shelljs": "^0.8.4"
  }
}
