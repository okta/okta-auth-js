<!DOCTYPE html>
<html>
  <head>
    {{>head}}
    <script src="/assets/js/poll.js"></script>
  </head>
  <body id="samples">
  
    {{>menu}}

    <div class="okta-sign-in-form ui center aligned relaxed grid">
      <div class="row">
        <div id="page-title-header" class="ui header">
          <h2>{{title}}</h2>
        </div>
      </div>
      {{>errors}}

      <div class="row">
        {{>formMessages}}
      </div>

      {{#contextualData}}
        <div class="ui segments">
          <div class="ui segment">
            {{#contextualData.qrcode}}
              <div class="ui fluid image">
                <img id="authenticator-qr-code" src="{{contextualData.qrcode.href}}" />
              </div>
            {{/contextualData.qrcode}}
          </div>
        </div>
      {{/contextualData}}
      <div class="row">
      {{>cancel}}
      </div>
      <div class="row">
      {{#nextStep}}
        <form action="{{action}}">
          <input type="hidden" value="{{nextStep}}"/>
          <button id="submit-button" class="ui fluid large primary submit button" type="submit">Can't Scan</button>
        </form>
      {{/nextStep}}
      </div>
    </div>
    {{#poll.required}}
      <script type="text/javascript">
        poll('{{{action}}}', {{poll.refresh}});
      </script>
    {{/poll.required}}
  </body>
</html>
