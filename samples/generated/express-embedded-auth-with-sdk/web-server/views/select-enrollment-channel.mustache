<!DOCTYPE html>
<html>
  <head>
    {{>head}}
  </head>
  <body id="samples">
  
    {{>menu}}

    <div class="okta-sign-in-form ui center aligned relaxed grid">
      <div class="row">
        <div id="select-enrollment-channel-page-title-header" class="ui header">
          <h2>Select Enrollment Channel</h2>
        </div>
      </div>

      <div class="row">
        {{>formMessages}}
      </div>

      <div class="row">
        <form id="verify-form" class="ui large form" action="{{action}}" method="POST">
          <div class="field">
            <div class="default text">Please choose enrollment channel for this authenticator</div>
            <select id="enrollment-channel-method-options" class="ui selection dropdown" name="channel">
              {{#options}}
                <option value="{{value}}">{{label}}</option>
              {{/options}}
            </select>
          </div>
          <button id="submit-button" class="ui fluid large primary submit button" type="submit">
            Next
          </button>
        </form>
      </div>

      {{>cancel}}

    </div>
  </body>
</html>
